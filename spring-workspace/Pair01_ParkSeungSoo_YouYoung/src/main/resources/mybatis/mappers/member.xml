<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip.model.dao.MemberDao">

    <select id="login" parameterType="Member" resultType="String">
        SELECT name
        FROM member
        WHERE id = #{id}
          AND password = #{password}
    </select>

    <select id="findByMemberId" parameterType="String" resultType="Member">
        SELECT name
        FROM member
        WHERE id = #{id}
    </select>

    <select id="findByMemberIdAndPassword" parameterType="Member" resultType="Member">
        SELECT name
        FROM member
        WHERE id = #{id}
          AND password = #{password}
    </select>


    <insert id="save" parameterType="Member">
        INSERT INTO member(memberOd, password, name, sido_code, gugun_code, profile_img, created_at, updated_at)
        VALUES (#{memberId}, #{password}, #{name}, #{email}, #{sidoCode}, #{gugunCode}, #{profileImg}, NOW(), NOW())
    </insert>

</mapper>